import { MaxValidationDirective } from './max-validation.directive';

describe('MaxValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxValidationDirective();
    expect(directive).toBeTruthy();
  });
});
