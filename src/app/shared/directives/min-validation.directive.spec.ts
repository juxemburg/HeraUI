import { MinValidationDirective } from './min-validation.directive';

describe('MinValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MinValidationDirective();
    expect(directive).toBeTruthy();
  });
});
